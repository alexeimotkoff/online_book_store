@model online_book_store.Domain.Entities.Book

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Price.ToString("# руб")</span>
    </h3>
    <h4>@Model.Author</h4>
    <span class="lead">@Model.Description</span>
</div>